# 西厂部署记录：剧情搜索与导出

- 开发分支：`feature/rebuild-from-public-initial`
- 基础提交：`5ab8c6b9c0`
- 本次提交：剧情搜索与导出稳定性修补

## 本次内容

1. 剧情搜索同时检查原始正文、渲染正文和剧情摘要，摘要关键词可以被检索。
2. 剧情导出统一清理 HTML，并保留段落、列表和换行结构，避免正文粘连。
3. 系统消息在搜索结果和导出文本中标记为“系统”，不再误显示为角色回复。
4. 保留原有完整剧本导出、自由勾选导出、定位跳转和高亮行为。

## 未修改范围

- ElevenLabs 接口、音频格式和后台播放策略。
- 线下语音、语气标签及结构化对白逻辑。
- 本地 API 配置、角色卡、世界书和聊天数据。
- `main` 分支。

## 验收提示

提交并触发 Netlify 更新后，浏览器执行一次硬刷新或关闭后重新打开应用即可看到版本。进入 `Story` → 菜单 → `剧情检索与剧本导出`，分别验证摘要关键词搜索、全本 TXT 和勾选 TXT。
